---
#
# Global variables from project settings:
#   PYPI_URL: The URL of the PyPI project, e.g. https://pypi.org/p/my-fancy-package
#   PYTHON_VERSION: The minimum Python version to support.
#

name: Publish Python 🐍 distribution 📦 to PyPI

# yamllint disable-line rule:truthy
on:
  release:
    types: [published]

jobs:
  default:
    uses: Hapag-Lloyd/Workflow-Templates/.github/workflows/python-publish.yml@91972445da9e92825c8c983c38c3b5ef9635dd68 # 1.19.1
    secrets: inherit
